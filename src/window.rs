//! Native Wayland window backend (ratatui-wgpu).
//!
//! This module is gated behind the `window` Cargo feature and will host the
//! winit + wgpu event loop that replaces the terminal backend. The full
//! implementation lands in a follow-up issue; for now the module is an empty
//! compile-path stub so the feature-flag scaffolding exists.
