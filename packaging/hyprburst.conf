# hyprburst.conf — drop-in Hyprland config for the hyprburst launcher.
#
# Source this file from your ~/.config/hypr/hyprland.conf:
#
#     source = ~/.config/hypr/hyprburst.conf
#
# Requires Hyprland 0.48+ (unified `windowrule`; `windowrulev2` is deprecated).

# Fullscreen overlay + semi-transparent blur for hyprburst.
windowrule = match:class ^(hyprburst)$, float on
windowrule = match:class ^(hyprburst)$, size (monitor_w) (monitor_h)
windowrule = match:class ^(hyprburst)$, move 0 0
windowrule = match:class ^(hyprburst)$, opacity 0.9 0.8
windowrule = match:class ^(hyprburst)$, border_size 0
windowrule = match:class ^(hyprburst)$, no_shadow on
windowrule = match:class ^(hyprburst)$, stay_focused on
windowrule = match:class ^(hyprburst)$, dim_around on

# Bind Super+Space to `hyprburst`, which resolves the user's terminal and
# re-execs hyprburst inside it with the right --class / --app-id flag so the
# windowrules above match. Use `hyprburst tui` to run inline instead.
bind = SUPER, Space, exec, hyprburst

# Optionally pin a specific terminal for hyprburst by exporting $TERMINAL to
# Hyprland. $TERMINAL is the only environment variable hyprburst respects in
# v1 — everything else lives in ~/.config/hyprburst/config.toml.
# env = TERMINAL,rio
